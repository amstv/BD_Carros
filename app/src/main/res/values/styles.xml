<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cores do Tema Material Design -->
        <!-- Cor primária da app -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Variação da cor primária para a status bar e contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Cor de acentuação para as views que representem a operação principal na tela -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Cor para o efeito ripple -->
        <item name="colorControlHighlight">@color/control_highlight</item>
    </style>

    <!-- Tema sem ActionBar (retira a ActionBar) -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
